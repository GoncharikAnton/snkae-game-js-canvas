class Directions {

    static allDirections = [
        [-1, 0], // left
        [1, 0], //right
        [0, -1], // top
        [0, 1], // bottom
    ];
    static left = [-1, 0];
    static right = [1, 0];
    static top = [0, -1];
    static bottom = [0, 1];
}

export default Directions;